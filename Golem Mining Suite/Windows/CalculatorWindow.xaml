<Window x:Class="Golem_Mining_Suite.CalculatorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cargo Value Calculator" Height="700" Width="600"
        Background="#1A1A1A"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#CCCCCC"/>
        </Style>
        
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#252525"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderBrush" Value="#333333"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="1"
                                          Focusable="False"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Style>
                                    <Style TargetType="ToggleButton">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ToggleButton">
                                                    <Border x:Name="Border" Background="{TemplateBinding Background}"
                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                            BorderThickness="{TemplateBinding BorderThickness}"
                                                            CornerRadius="4">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition/>
                                                                <ColumnDefinition Width="20"/>
                                                            </Grid.ColumnDefinitions>
                                                            <Path Grid.Column="1" x:Name="Arrow"
                                                                  HorizontalAlignment="Center" VerticalAlignment="Center"
                                                                  Data="M 0 0 L 4 4 L 8 0 Z"
                                                                  Fill="#CCCCCC"/>
                                                        </Grid>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="Border" Property="BorderBrush" Value="#FF8C42"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ToggleButton.Style>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite"
                                              IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="8,3,23,3"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"/>
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Grid x:Name="DropDown"
                                      SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                            Background="#2A2A2A"
                                            BorderBrush="#FF8C42"
                                            BorderThickness="1"
                                            CornerRadius="4">
                                        <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="#CCCCCC"/>
                        <Setter Property="Padding" Value="8,6"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="Border"
                                            Background="{TemplateBinding Background}"
                                            Padding="{TemplateBinding Padding}"
                                            CornerRadius="2"
                                            Margin="2">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsHighlighted" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="#3D3528"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="#FF8C42"/>
                                            <Setter Property="Foreground" Value="#1A1A1A"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#252525"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderBrush" Value="#333333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="CaretBrush" Value="#FF8C42"/>
            <Setter Property="Height" Value="35"/>
        </Style>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#252525"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderBrush" Value="#333333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2A2A2A"/>
                    <Setter Property="BorderBrush" Value="#FF8C42"/>
                    <Setter Property="Foreground" Value="#FF8C42"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header with Compare Button -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Cargo Value Calculator" 
                       FontSize="24" FontWeight="Bold" 
                       Foreground="#FF8C42" 
                       VerticalAlignment="Center"/>
            <Button x:Name="CompareButton" Content="📊 Compare Minerals" 
                    HorizontalAlignment="Right"
                    Style="{StaticResource ActionButton}"
                    Click="CompareButton_Click"/>
        </Grid>

        <!-- Ship Selection & Cargo Capacity -->
        <Border Grid.Row="1" Background="#252525" BorderBrush="#333333" 
                BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="Ship:" VerticalAlignment="Center"/>
                    <ComboBox x:Name="ShipComboBox" Grid.Column="2"
                              SelectionChanged="ShipComboBox_SelectionChanged"/>
                </Grid>
                
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <ProgressBar x:Name="CargoProgressBar" Grid.Column="0"
                                 Height="20" Minimum="0" Maximum="100" Value="0"
                                 Foreground="#FF8C42" Background="#1A1A1A"/>
                    
                    <TextBlock x:Name="CargoCapacityText" Grid.Column="2"
                               Text="0 / 32 SCU" 
                               VerticalAlignment="Center"
                               Foreground="#FF8C42" FontWeight="Bold"/>
                </Grid>
            </Grid>
        </Border>

        <!-- Station Selection -->
        <Border Grid.Row="2" Background="#252525" BorderBrush="#333333" 
                BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" Text="Selling Station:" VerticalAlignment="Center"/>
                <ComboBox x:Name="StationComboBox" Grid.Column="2"
                          SelectionChanged="StationComboBox_SelectionChanged"/>
            </Grid>
        </Border>

        <!-- Mineral Input Grid -->
        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto" Margin="0,0,0,15">
            <StackPanel x:Name="MineralInputPanel">
                <!-- Mineral Rows (5 total) -->
                <Border Background="#252525" BorderBrush="#333333" 
                        BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,0,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="40"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="5"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Mineral 1:" Margin="0,0,0,5"/>
                        <ComboBox x:Name="Mineral1ComboBox" Grid.Row="2" Grid.Column="0"
                                  SelectionChanged="MineralComboBox_SelectionChanged"/>
                        
                        <TextBlock Grid.Row="0" Grid.Column="2" Text="SCU Amount:" Margin="0,0,0,5"/>
                        <TextBox x:Name="SCU1TextBox" Grid.Row="2" Grid.Column="2"
                                 Text="0" TextChanged="SCUTextBox_TextChanged"/>
                        
                        <Button x:Name="Clear1Button" Grid.Row="2" Grid.Column="4" Content="✖"
                                Style="{StaticResource ActionButton}" Padding="8"
                                Click="ClearRow_Click" Tag="1"/>
                    </Grid>
                </Border>

                <Border Background="#252525" BorderBrush="#333333" 
                        BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,0,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="40"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="5"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Mineral 2:" Margin="0,0,0,5"/>
                        <ComboBox x:Name="Mineral2ComboBox" Grid.Row="2" Grid.Column="0"
                                  SelectionChanged="MineralComboBox_SelectionChanged"/>
                        
                        <TextBlock Grid.Row="0" Grid.Column="2" Text="SCU Amount:" Margin="0,0,0,5"/>
                        <TextBox x:Name="SCU2TextBox" Grid.Row="2" Grid.Column="2"
                                 Text="0" TextChanged="SCUTextBox_TextChanged"/>
                        
                        <Button x:Name="Clear2Button" Grid.Row="2" Grid.Column="4" Content="✖"
                                Style="{StaticResource ActionButton}" Padding="8"
                                Click="ClearRow_Click" Tag="2"/>
                    </Grid>
                </Border>

                <Border Background="#252525" BorderBrush="#333333" 
                        BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,0,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="40"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="5"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Mineral 3:" Margin="0,0,0,5"/>
                        <ComboBox x:Name="Mineral3ComboBox" Grid.Row="2" Grid.Column="0"
                                  SelectionChanged="MineralComboBox_SelectionChanged"/>
                        
                        <TextBlock Grid.Row="0" Grid.Column="2" Text="SCU Amount:" Margin="0,0,0,5"/>
                        <TextBox x:Name="SCU3TextBox" Grid.Row="2" Grid.Column="2"
                                 Text="0" TextChanged="SCUTextBox_TextChanged"/>
                        
                        <Button x:Name="Clear3Button" Grid.Row="2" Grid.Column="4" Content="✖"
                                Style="{StaticResource ActionButton}" Padding="8"
                                Click="ClearRow_Click" Tag="3"/>
                    </Grid>
                </Border>

                <Border Background="#252525" BorderBrush="#333333" 
                        BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,0,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="40"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="5"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Mineral 4:" Margin="0,0,0,5"/>
                        <ComboBox x:Name="Mineral4ComboBox" Grid.Row="2" Grid.Column="0"
                                  SelectionChanged="MineralComboBox_SelectionChanged"/>
                        
                        <TextBlock Grid.Row="0" Grid.Column="2" Text="SCU Amount:" Margin="0,0,0,5"/>
                        <TextBox x:Name="SCU4TextBox" Grid.Row="2" Grid.Column="2"
                                 Text="0" TextChanged="SCUTextBox_TextChanged"/>
                        
                        <Button x:Name="Clear4Button" Grid.Row="2" Grid.Column="4" Content="✖"
                                Style="{StaticResource ActionButton}" Padding="8"
                                Click="ClearRow_Click" Tag="4"/>
                    </Grid>
                </Border>

                <Border Background="#252525" BorderBrush="#333333" 
                        BorderThickness="1" CornerRadius="4" Padding="15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="40"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="5"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Mineral 5:" Margin="0,0,0,5"/>
                        <ComboBox x:Name="Mineral5ComboBox" Grid.Row="2" Grid.Column="0"
                                  SelectionChanged="MineralComboBox_SelectionChanged"/>
                        
                        <TextBlock Grid.Row="0" Grid.Column="2" Text="SCU Amount:" Margin="0,0,0,5"/>
                        <TextBox x:Name="SCU5TextBox" Grid.Row="2" Grid.Column="2"
                                 Text="0" TextChanged="SCUTextBox_TextChanged"/>
                        
                        <Button x:Name="Clear5Button" Grid.Row="2" Grid.Column="4" Content="✖"
                                Style="{StaticResource ActionButton}" Padding="8"
                                Click="ClearRow_Click" Tag="5"/>
                    </Grid>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Summary Panel -->
        <Border Grid.Row="4" Background="#2A2A2A" BorderBrush="#FF8C42" 
                BorderThickness="2" CornerRadius="6" Padding="20">
            <StackPanel>
                <TextBlock x:Name="TotalValueText" 
                           Text="Total Value: 0 aUEC" 
                           FontSize="20" FontWeight="Bold" Foreground="#4CAF50"
                           Margin="0,0,0,10"/>
                
                <TextBlock x:Name="BestLocationText" 
                           Text="💰 Best Price At: Port Tressler" 
                           FontSize="12" Foreground="#FFD700"
                           Margin="0,0,0,5"/>
                
                <TextBlock x:Name="PricePerSCUText" 
                           Text="Average: 0 aUEC/SCU" 
                           FontSize="12" Foreground="#999999"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>

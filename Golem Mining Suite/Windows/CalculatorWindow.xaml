<Window x:Class="Golem_Mining_Suite.Windows.CalculatorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cargo Value Calculator" Height="700" Width="600"
        Background="{StaticResource PrimaryBackgroundBrush}"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">

    <Window.Resources>
        <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}"/>
        <Style TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}"/>
        <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}"/>
        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header with Compare Button -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Cargo Value Calculator" 
                       FontSize="24" FontWeight="Bold" 
                       Foreground="{StaticResource AccentBrush}" 
                       VerticalAlignment="Center"/>
            <Button Content="📊 Compare Minerals" 
                    HorizontalAlignment="Right"
                    Command="{Binding OpenComparisonCommand}"/>
        </Grid>

        <!-- Ship Selection & Cargo Capacity -->
        <Border Grid.Row="1" Background="{StaticResource SecondaryBackgroundBrush}" BorderBrush="{StaticResource BorderBrush}" 
                BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="Ship:" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <ComboBox Grid.Column="2"
                              ItemsSource="{Binding Ships}"
                              SelectedItem="{Binding SelectedShip}"/>
                </Grid>
                
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <ProgressBar Grid.Column="0"
                                 Height="20" Minimum="0" Maximum="100" 
                                 Value="{Binding CapacityPercentage}"
                                 Foreground="{StaticResource AccentBrush}" 
                                 Background="{StaticResource PrimaryBackgroundBrush}"
                                 BorderBrush="{StaticResource BorderBrush}"
                                 BorderThickness="1"/>
                    
                    <TextBlock Grid.Column="2"
                               Text="{Binding CapacityText}" 
                               VerticalAlignment="Center"
                               FontWeight="Bold">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsOverCapacity}" Value="True">
                                        <Setter Property="Foreground" Value="#F44336"/> <!-- Red -->
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
            </Grid>
        </Border>

        <!-- Station Selection -->
        <Border Grid.Row="2" Background="{StaticResource SecondaryBackgroundBrush}" BorderBrush="{StaticResource BorderBrush}" 
                BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" Text="Selling Station:" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                <ComboBox Grid.Column="2"
                          ItemsSource="{Binding Stations}"
                          SelectedItem="{Binding SelectedStation}"/>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto" Margin="0,0,0,15">
            <ItemsControl ItemsSource="{Binding MineralRows}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{StaticResource SecondaryBackgroundBrush}" BorderBrush="{StaticResource BorderBrush}" 
                                BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="40"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="5"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <TextBlock Grid.Row="0" Grid.Column="0" Text="Mineral:" Margin="0,0,0,5" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <ComboBox Grid.Row="2" Grid.Column="0"
                                          ItemsSource="{Binding Minerals}"
                                          SelectedItem="{Binding SelectedMineral}"/>
                                
                                <TextBlock Grid.Row="0" Grid.Column="2" Text="SCU Amount:" Margin="0,0,0,5" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBox Grid.Row="2" Grid.Column="2"
                                         Text="{Binding ScuText, UpdateSourceTrigger=PropertyChanged}"/>
                                
                                <Button Grid.Row="2" Grid.Column="4" Content="✖"
                                        Padding="0"
                                        Command="{Binding ClearCommand}"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Foreground="#F44336"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Summary Panel -->
        <Border Grid.Row="4" Background="{StaticResource SecondaryBackgroundBrush}" BorderBrush="{StaticResource AccentBrush}" 
                BorderThickness="1" CornerRadius="6" Padding="20">
            <StackPanel>
                <TextBlock Text="{Binding TotalValueText}" 
                           FontSize="20" FontWeight="Bold" Foreground="#4CAF50"
                           Margin="0,0,0,10"/>
                
                <TextBlock Text="{Binding BestLocationText}" 
                           FontSize="12" Foreground="#FFC107"
                           Margin="0,0,0,5"/>
                
                <TextBlock Text="{Binding PricePerSCUText}" 
                           FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>

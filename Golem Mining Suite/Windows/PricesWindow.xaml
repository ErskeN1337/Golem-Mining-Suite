<Window x:Class="Golem_Mining_Suite.Windows.PricesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Golem_Mining_Suite.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:PricesViewModel}"
        Title="Mineral Prices" Height="600" Width="900"
        Background="{DynamicResource PrimaryBackgroundBrush}"
        BorderBrush="{DynamicResource BorderBrush}"
        BorderThickness="1"
        WindowStartupLocation="CenterOwner">
        
    <d:Window.DataContext>
        <vm:PricesViewModel/>
    </d:Window.DataContext>

    <x:Code>
        <![CDATA[
            private void Window_Loaded(object sender, System.Windows.RoutedEventArgs e)
            {
                if (DataContext is Golem_Mining_Suite.ViewModels.PricesViewModel vm)
                {
                    vm.LoadDataCommand.Execute(null);
                }
            }
        ]]>
    </x:Code>

    <Window.Resources>
        <Style x:Key="FilterButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Padding" Value="10,2"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Filters (Row 0) -->
        <Border Grid.Row="0" Background="{DynamicResource SecondaryBackgroundBrush}" CornerRadius="4" Padding="10" Margin="0,0,0,10" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="System:" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="Bold"/>
                
                <Button Content="All" Width="80" 
                        Command="{Binding SetSystemFilterCommand}" CommandParameter="All">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource FilterButton}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedSystem}" Value="All">
                                    <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
                                    <Setter Property="Foreground" Value="#1E1E1E"/>
                                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="Stanton" Width="80" 
                        Command="{Binding SetSystemFilterCommand}" CommandParameter="Stanton">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource FilterButton}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedSystem}" Value="Stanton">
                                    <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
                                    <Setter Property="Foreground" Value="#1E1E1E"/>
                                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="Pyro" Width="80" 
                        Command="{Binding SetSystemFilterCommand}" CommandParameter="Pyro">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource FilterButton}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedSystem}" Value="Pyro">
                                    <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
                                    <Setter Property="Foreground" Value="#1E1E1E"/>
                                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <TextBlock Text="Mineral:" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="20,0,10,0" FontWeight="Bold"/>
                
                <ComboBox Width="150"
                          ItemsSource="{Binding Minerals}"
                          SelectedItem="{Binding SelectedMineral}"/>
            </StackPanel>
        </Border>

        <!-- Prices Grid -->
        <DataGrid Name="PricesGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  Background="{DynamicResource SecondaryBackgroundBrush}"
                  Foreground="{DynamicResource TextPrimaryBrush}"
                  RowBackground="{DynamicResource SecondaryBackgroundBrush}"
                  AlternatingRowBackground="#303030"
                  BorderBrush="{DynamicResource BorderBrush}"
                  BorderThickness="1"
                  IsReadOnly="True"
                  ItemsSource="{Binding Prices}"
                  CanUserAddRows="False">
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#333333"/>
                    <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                    <Setter Property="Padding" Value="10,8"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                </Style>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="10,5"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                    <ContentPresenter/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                             <Setter Property="Background" Value="#444444"/>
                             <Setter Property="Foreground" Value="White"/>
                             <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Resources>
            
            <DataGrid.Columns>
                <DataGridTextColumn Header="MINERAL" Binding="{Binding MineralName}" Width="150" FontWeight="SemiBold"/>
                <DataGridTextColumn Header="PRICE (SELL)" Binding="{Binding Price}" Width="120"/>
                <DataGridTextColumn Header="BEST LOCATION" Binding="{Binding BestLocation}" Width="*"/>
                <DataGridTextColumn Header="DEMAND" Binding="{Binding Demand}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Demand}" Value="Live">
                                    <Setter Property="Foreground" Value="#00BCD4"/> <!-- Cyan for Live -->
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Demand}" Value="High">
                                    <Setter Property="Foreground" Value="#4CAF50"/> <!-- Green -->
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Demand}" Value="Medium">
                                    <Setter Property="Foreground" Value="#FFC107"/> <!-- Amber -->
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Demand}" Value="Low">
                                    <Setter Property="Foreground" Value="#F44336"/> <!-- Red -->
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="UPDATED" Binding="{Binding LastUpdatedText}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status Text -->
        <Border Grid.Row="2" Background="#252525" Margin="0,10,0,0" Padding="10,5" CornerRadius="4" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <TextBlock Text="STATUS:" Foreground="{DynamicResource AccentBrush}" FontWeight="Bold" Margin="0,0,5,0"/>
                    <TextBlock Text="{Binding StatusText}" Foreground="{DynamicResource TextSecondaryBrush}"/>
                </StackPanel>
                
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Visibility="Collapsed">
                    <Ellipse Width="10" Height="10" Margin="0,0,5,0" VerticalAlignment="Center">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Setter Property="Fill" Value="#757575"/> <!-- Gray default -->
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsLiveConnected}" Value="True">
                                        <Setter Property="Fill" Value="#4CAF50"/> <!-- Green -->
                                    </DataTrigger>
                                    <!-- Fallback/Pulse animation could go here -->
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock Text="Live Network" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" FontSize="11"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>

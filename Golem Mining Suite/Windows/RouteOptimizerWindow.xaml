<Window x:Class="Golem_Mining_Suite.Windows.RouteOptimizerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Golem_Mining_Suite.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:RouteOptimizerViewModel}"
        Title="Trade Route Optimizer" Height="700" Width="1100"
        Background="{DynamicResource PrimaryBackgroundBrush}"
        BorderBrush="{DynamicResource BorderBrush}"
        BorderThickness="1"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Trade Route Optimizer" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Foreground="{DynamicResource TextPrimaryBrush}"
                   Margin="0,0,0,15"/>

        <!-- Filters Panel -->
        <Border Grid.Row="1" 
                Background="{DynamicResource SecondaryBackgroundBrush}" 
                BorderBrush="{DynamicResource BorderBrush}" 
                BorderThickness="1" 
                CornerRadius="6" 
                Padding="15"
                Margin="0,0,0,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="250"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Ship Selection -->
                <TextBlock Grid.Row="0" Grid.Column="0" 
                           Text="Ship:" 
                           VerticalAlignment="Center" 
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           FontWeight="Bold"
                           Margin="0,0,10,0"/>
                <ComboBox Grid.Row="0" Grid.Column="1"
                          ItemsSource="{Binding Ships}"
                          SelectedItem="{Binding SelectedShip}"
                          Width="200"
                          HorizontalAlignment="Left"/>

                <!-- System Filter -->
                <!-- From System -->
                <TextBlock Grid.Row="0" Grid.Column="3" 
                           Text="From:" 
                           VerticalAlignment="Center" 
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           FontWeight="Bold"
                           Margin="0,0,10,0"/>
                <ComboBox Grid.Row="0" Grid.Column="4"
                          ItemsSource="{Binding Systems}"
                          SelectedItem="{Binding SelectedSourceSystem}"/>

                <!-- To System -->
                <TextBlock Grid.Row="0" Grid.Column="5" 
                           Text=" To:" 
                           VerticalAlignment="Center" 
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           FontWeight="Bold"
                           Margin="10,0,10,0"
                           HorizontalAlignment="Left"/>
                <ComboBox Grid.Row="0" Grid.Column="5"
                          ItemsSource="{Binding Systems}"
                          SelectedItem="{Binding SelectedTargetSystem}"
                          Width="120"
                          HorizontalAlignment="Left"
                          Margin="40,0,0,0"/>

                <!-- Refresh Button -->
                <Button Grid.Row="0" Grid.Column="6"
                        Content="🔄 Refresh Routes"
                        Command="{Binding RefreshRoutesCommand}"
                        Padding="15,8"
                        FontWeight="Bold"/>

                <!-- Cargo Capacity Display -->
                <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" 
                            Orientation="Horizontal" 
                            Margin="0,10,0,0">
                    <TextBlock Text="Cargo Capacity: " 
                               Foreground="{DynamicResource TextSecondaryBrush}"/>
                    <TextBlock Text="{Binding CargoCapacity}" 
                               Foreground="{DynamicResource AccentBrush}"
                               FontWeight="Bold"/>
                    <TextBlock Text=" SCU" 
                               Foreground="{DynamicResource TextSecondaryBrush}"/>
                </StackPanel>

                <!-- Commodity Filter -->
                <StackPanel Grid.Row="1" Grid.Column="3" Grid.ColumnSpan="2" 
                            Orientation="Horizontal" 
                            Margin="0,10,0,0">
                    <TextBlock Text="Commodity: " 
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               FontWeight="Bold"
                               Margin="0,0,10,0"/>
                    <ComboBox Width="150" 
                              ItemsSource="{Binding Commodities}"
                              SelectedItem="{Binding SelectedCommodity}"
                              VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Min Profit Filter -->
                <StackPanel Grid.Row="1" Grid.Column="6" 
                            Orientation="Horizontal" 
                            Margin="0,10,0,0">
                    <TextBlock Text="Min Profit: " 
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               Margin="0,0,10,0"/>
                    <TextBox Width="100" 
                             Text="{Binding MinProfit, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}"
                             VerticalAlignment="Center">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Return" Command="{Binding ApplyFiltersCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>
                    <TextBlock Text=" aUEC" 
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               Margin="5,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Routes DataGrid -->
        <DataGrid Grid.Row="2"
                  AutoGenerateColumns="False"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  Background="{DynamicResource SecondaryBackgroundBrush}"
                  Foreground="{DynamicResource TextPrimaryBrush}"
                  RowBackground="{DynamicResource SecondaryBackgroundBrush}"
                  AlternatingRowBackground="#303030"
                  BorderBrush="{DynamicResource BorderBrush}"
                  BorderThickness="1"
                  IsReadOnly="True"
                  ItemsSource="{Binding Routes}"
                  CanUserAddRows="False">
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#333333"/>
                    <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                    <Setter Property="Padding" Value="10,8"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                </Style>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="10,5"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Padding="{TemplateBinding Padding}" 
                                        Background="{TemplateBinding Background}" 
                                        BorderBrush="{TemplateBinding BorderBrush}" 
                                        BorderThickness="{TemplateBinding BorderThickness}">
                                    <ContentPresenter/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#444444"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn Header="COMMODITY" Binding="{Binding CommodityName}" Width="150" FontWeight="SemiBold"/>
                <DataGridTextColumn Header="BUY FROM" Binding="{Binding BuyStation}" Width="*"/>
                <DataGridTextColumn Header="BUY PRICE" Binding="{Binding BuyPriceFormatted}" Width="120"/>
                <DataGridTextColumn Header="SELL TO" Binding="{Binding SellStation}" Width="*"/>
                <DataGridTextColumn Header="SELL PRICE" Binding="{Binding SellPriceFormatted}" Width="120"/>
                <DataGridTextColumn Header="PROFIT/SCU" Binding="{Binding ProfitPerSCUFormatted}" Width="130">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="TOTAL PROFIT" Binding="{Binding TotalProfitFormatted}" Width="150">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#4CAF50"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="FontSize" Value="14"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="SYSTEM" Binding="{Binding SystemRoute}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status Bar -->
        <Border Grid.Row="3" 
                Background="#252525" 
                Margin="0,15,0,0" 
                Padding="10,5" 
                CornerRadius="4" 
                BorderBrush="{DynamicResource BorderBrush}" 
                BorderThickness="1">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <TextBlock Text="STATUS:" 
                               Foreground="{DynamicResource AccentBrush}" 
                               FontWeight="Bold" 
                               Margin="0,0,5,0"/>
                    <TextBlock Text="{Binding StatusText}" 
                               Foreground="{DynamicResource TextSecondaryBrush}"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
                    <TextBlock Text="⏳ Loading..." 
                               Foreground="{DynamicResource AccentBrush}" 
                               FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>

<UserControl x:Class="Golem_Mining_Suite.Views.CrewSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Golem_Mining_Suite.ViewModels"
             xmlns:util="clr-namespace:Golem_Mining_Suite.Utilities"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:CrewSessionViewModel}"
             Background="Transparent">

    <UserControl.Resources>
        <!-- Local converter — inverts IsBusy into IsEnabled for the toolbar buttons. -->
        <util:InverseBooleanConverter x:Key="BoolToEnabledConverter"/>

        <!-- Toolbar button style — matches the visual language of HaulingDashboardView -->
        <Style x:Key="ToolbarButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource SecondaryBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="#1E1E1E"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Header -->
            <RowDefinition Height="Auto"/>  <!-- Toolbar -->
            <RowDefinition Height="*"/>     <!-- DataGrid / empty state -->
            <RowDefinition Height="Auto"/>  <!-- Status bar -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,20">
            <TextBlock Text="Crew Sessions"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="{DynamicResource AccentBrush}"
                       HorizontalAlignment="Center"/>
            <TextBlock Text="Import and review crew mining sessions from Regolith"
                       FontSize="14"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       HorizontalAlignment="Center"
                       Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Toolbar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <Button Style="{StaticResource ToolbarButton}"
                    Content="📄 Import from Regolith File"
                    Command="{Binding ImportFromRegolithFileCommand}"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource BoolToEnabledConverter}, FallbackValue=True}"/>
            <Button Style="{StaticResource ToolbarButton}"
                    Content="🌐 Import from Regolith API"
                    Command="{Binding ImportFromRegolithApiCommand}"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource BoolToEnabledConverter}, FallbackValue=True}"/>
            <Button Style="{StaticResource ToolbarButton}"
                    Content="🗑 Delete"
                    Command="{Binding DeleteSelectedCommand}"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource BoolToEnabledConverter}, FallbackValue=True}"/>
            <Button Style="{StaticResource ToolbarButton}"
                    Content="🔄 Refresh"
                    Command="{Binding RefreshCommand}"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource BoolToEnabledConverter}, FallbackValue=True}"/>
        </StackPanel>

        <!-- Data Grid -->
        <Border Grid.Row="2"
                Background="{DynamicResource SecondaryBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="1">
            <Grid>
                <DataGrid ItemsSource="{Binding Sessions}"
                          SelectedItem="{Binding SelectedSession}"
                          AutoGenerateColumns="False"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          CanUserReorderColumns="True"
                          CanUserResizeColumns="True"
                          CanUserSortColumns="True"
                          IsReadOnly="True"
                          Background="Transparent"
                          BorderThickness="0"
                          Foreground="{DynamicResource TextPrimaryBrush}"
                          RowBackground="{DynamicResource SecondaryBackgroundBrush}"
                          AlternatingRowBackground="#303030"
                          HorizontalGridLinesBrush="{DynamicResource BorderBrush}"
                          SelectionMode="Single"
                          SelectionUnit="FullRow">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#333333"/>
                            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                            <Setter Property="Padding" Value="10,8"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="BorderThickness" Value="0,0,1,1"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                        </Style>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="10,5"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                            <ContentPresenter/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#444444"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" MinWidth="180"/>
                        <DataGridTextColumn Header="Started" Binding="{Binding StartedDisplay}" Width="Auto" MinWidth="130"/>
                        <DataGridTextColumn Header="Crew" Binding="{Binding CrewCount}" Width="Auto" MinWidth="60"/>
                        <DataGridTextColumn Header="Total aUEC" Binding="{Binding TotalAuecDisplay}" Width="Auto" MinWidth="130"/>
                        <DataGridTextColumn Header="My Share" Binding="{Binding MyShareDisplay}" Width="Auto" MinWidth="130"/>
                        <DataGridTextColumn Header="Source" Binding="{Binding SourceTool}" Width="Auto" MinWidth="90"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Empty state overlay: appears when there are zero rows and we're not busy loading.
                     IsHitTestVisible=False lets the DataGrid still receive focus / clicks. -->
                <StackPanel HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            IsHitTestVisible="False">
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Sessions.Count}" Value="0">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>
                    <TextBlock Text="⛏"
                               FontSize="48"
                               HorizontalAlignment="Center"
                               Opacity="0.5"
                               Foreground="{DynamicResource TextSecondaryBrush}"/>
                    <TextBlock Text="No crew sessions yet"
                               FontSize="18"
                               FontWeight="SemiBold"
                               HorizontalAlignment="Center"
                               Margin="0,10,0,5"
                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                    <TextBlock Text="Click 'Import from Regolith File' or 'Import from Regolith API' to get started."
                               FontSize="13"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource TextSecondaryBrush}"/>
                </StackPanel>

                <!-- Busy overlay -->
                <Grid Background="#88000000"
                      Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}, FallbackValue=Collapsed}">
                    <TextBlock Text="Working..."
                               Foreground="White"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               FontSize="18"
                               FontWeight="Bold"/>
                </Grid>
            </Grid>
        </Border>

        <!-- Status Bar -->
        <Border Grid.Row="3"
                Background="{DynamicResource SecondaryBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="6"
                Padding="12,8"
                Margin="0,10,0,0">
            <TextBlock Text="{Binding StatusText}"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       FontSize="12"
                       TextWrapping="Wrap"/>
        </Border>
    </Grid>
</UserControl>

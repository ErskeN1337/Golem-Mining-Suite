<Window x:Class="Golem_Mining_Suite.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Golem Mining Suite"
        Width="280"
        Height="550"
        Background="#1A1A1A"
        ResizeMode="NoResize"
        Loaded="Window_Loaded">

    <Window.Resources>
        <Style x:Key="MenuButton" TargetType="Button">
            <Setter Property="Background" Value="#252525"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderBrush" Value="#333333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3D3528"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF8C42"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#333333"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Border Background="#1A1A1A" BorderBrush="#2A2A2A" BorderThickness="1">
            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,10,0,25">
                    <Image Source="pack://application:,,,/Images/GolemMiningSuite_logo.png" 
                           Height="67" 
                           Stretch="Uniform"
                           Width="232">
                        <Image.Effect>
                            <DropShadowEffect Color="#FF8C42" 
                                              BlurRadius="15" 
                                              ShadowDepth="0" 
                                              Opacity="0.3"/>
                        </Image.Effect>
                    </Image>
                </StackPanel>

                <Border Grid.Row="1" 
                        Background="#252525" 
                        BorderBrush="#333333" 
                        BorderThickness="1"
                        CornerRadius="4"
                        Margin="0,0,0,25"
                        x:Name="SearchBorder">
                    <Grid>
                        <TextBox Name="SearchBox"
                                 Text="Search mineral..."
                                 FontSize="14"
                                 Background="Transparent"
                                 Foreground="#999999"
                                 BorderThickness="0"
                                 Padding="12,10"
                                 KeyUp="SearchBox_KeyUp"
                                 GotFocus="SearchBox_GotFocus"
                                 LostFocus="SearchBox_LostFocus"/>

                        <ListBox Name="SuggestionsListBox"
                                 Margin="0,45,0,0"
                                 Background="#2A2A2A"
                                 Foreground="#CCCCCC"
                                 BorderBrush="#333333"
                                 BorderThickness="1"
                                 Visibility="Collapsed"
                                 MaxHeight="150"
                                 MouseLeftButtonUp="SuggestionsListBox_MouseLeftButtonUp">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Background" Value="#2A2A2A"/>
                                    <Setter Property="Foreground" Value="#CCCCCC"/>
                                    <Setter Property="Padding" Value="12,8"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#333333"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>
                    </Grid>
                </Border>

                <StackPanel Grid.Row="2">
                    <Button Content="💎 Mineral Prices"
                            Click="MineralPrices_Click"
                            Style="{StaticResource MenuButton}"/>

                    <Button Content="📦 Cargo Calculator"
                            Click="CargoCalculator_Click"
                            Style="{StaticResource MenuButton}"/>
                </StackPanel>

                <Grid Grid.Row="3" Margin="0,20,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Separator Grid.Row="0" 
                               Background="#2A2A2A" 
                               Margin="0,0,0,15"/>

                    <Grid Grid.Row="1">
                        <TextBlock Name="VersionText"
                                   Text="v1.0.6"
                                   Foreground="#666666"
                                   FontSize="11"
                                   HorizontalAlignment="Left"
                                   VerticalAlignment="Bottom"
                                   Margin="0,0,0,5"/>

                        <Button x:Name="UexLinkButton"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Bottom"
                                Background="Transparent"
                                BorderThickness="0"
                                Cursor="Hand"
                                Click="UexLinkButton_Click"
                                Padding="0"
                                Margin="0,0,0,-5">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Opacity" Value="0.5"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="Transparent">
                                                    <ContentPresenter/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Opacity" Value="1.0"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <Image Source="Images/uex-logo.png" 
                                   Height="30" 
                                   Width="85"
                                   Stretch="Uniform"/>
                        </Button>
                    </Grid>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>